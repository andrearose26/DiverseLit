import React from "react";

const LoadingScreen = () => {
    return (
        <div className="loadingScreen">
            <h1>Grabbing some books ...</h1>
        </div>
    );
};

export default LoadingScreen;